# Payment service sample

This small, original repository models a service that sends one payment
attempt through a gateway client and returns the resulting authorization. Its
glossary is already settled so the automated walkthrough can demonstrate the
complete machine-side lifecycle without pretending that software made the
naming decisions.

The sample is covered by Glossarize's Apache-2.0 license.
