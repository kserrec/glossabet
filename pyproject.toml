[build-system]
requires = ["hatchling>=1.32,<1.33"]
build-backend = "hatchling.build"

[project]
name = "glossarize"
description = "Make a codebase's vocabulary explicit, canonical, inspectable, and maintainable."
readme = "README.md"
license = "Apache-2.0"
authors = [{ name = "Kyle Serrecchia" }]
requires-python = ">=3.10"
keywords = ["code-quality", "developer-tools", "glossary", "terminology"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Environment :: Console",
  "Intended Audience :: Developers",
  "Operating System :: OS Independent",
  "Programming Language :: Python :: 3 :: Only",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Programming Language :: Python :: 3.14",
  "Topic :: Software Development :: Quality Assurance",
]
dynamic = ["version"]

[project.urls]
Homepage = "https://github.com/kserrec/glossarize"
Repository = "https://github.com/kserrec/glossarize"
Issues = "https://github.com/kserrec/glossarize/issues"
Changelog = "https://github.com/kserrec/glossarize/blob/main/CHANGELOG.md"

[project.scripts]
glossarize = "glossarize.cli:main"

[dependency-groups]
dev = ["pytest"]

[tool.hatch.version]
path = "glossarize/__init__.py"

[tool.hatch.build.targets.wheel]
packages = ["glossarize"]

[tool.hatch.build.targets.wheel.force-include]
"skill/SKILL.md" = "glossarize/_skill/SKILL.md"

[tool.hatch.build.targets.sdist]
include = [
  "/glossarize",
  "/skill",
  "/tests",
  "/evaluation",
  "/examples",
  "/docs",
  "/scripts",
  "/.github/workflows",
  "/ARCHITECTURE.md",
  "/CHANGELOG.md",
  "/CLAUDE.md",
  "/EVALUATION.md",
  "/LICENSE",
  "/PLAN.md",
  "/PLAN-ARCHIVE.md",
  "/PRIVACY.md",
  "/README.md",
  "/RELEASING.md",
  "/SECURITY.md",
  "/pyproject.toml",
  "/uv.lock",
]
